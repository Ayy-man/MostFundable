# Fix before proper launch

Release: `9d290a3fb01ac5f656179f7946d2f4df9ece1161`
Measured against the live deployment on 2026-09-02 by signing in as all four roles and reading the responses. Source references re-checked against `main` on 2026-09-05; the live measurements quoted are still the 2026-09-02 ones. Line references are to this repository.

The deployment is running in a deliberate demo configuration. Every item below is either a demo-mode behaviour that is fine for testing but must change before real money, real email or real credit data flow, or a gap found while mapping the build. Demo testing can continue with all of these open.

## A. Demo-mode behaviours to switch off before launch

| # | What happens today | Why it is acceptable for demo | What must change | Proof |
| --- | --- | --- | --- | --- |
| A1 | Every consumer charge succeeds without a payment provider. The seeded consumer shows an active 49 dollar subscription that was never charged. | Lets the enrollment and activation flow be walked end to end. | Set `BILLING_DRIVER=stripe` with the five Stripe keys and run the enrollment walk against a real test card. Confirm the subscription row carries a real provider reference. | `web/src/lib/billing/mock.ts:213-228`; `web/src/lib/env.ts:87-100`; live `GET /api/enroll` shows `subscription.status: active` |
| A2 | The card step in onboarding stores nothing. The mock setup intent returns no client secret and the browser never sends card digits anywhere. | Nothing sensitive is captured during demos. | Wire the real card capture for the Stripe arm and verify `confirmCard` with an attached payment method. | `web/src/components/onboarding1.tsx:554-580`; `web/src/lib/billing/mock.ts:190-196`; `web/src/lib/billing/stripe.ts:162` unverified |
| A3 | The paid credit refresh is priced and shown but refuses to charge in production until both Stripe and the bureau are live. | Correct fail-closed behaviour. | Follows automatically from A1 plus C1. Verify a 19 dollar refresh purchase once both are on. | `web/src/lib/pricing/paid-refresh-availability.ts:9-17`; live `POST /api/refresh-now` returns 503 |
| A4 | Product email is live on production since 2026-09-06: `EMAIL_DRIVER=resend`, `FEATURE_EMAIL=1`, `EMAIL_FROM_ADDRESS=notifications@mail.mostfundable.com` (the verified Resend domain is `mail.mostfundable.com`), a full-access Resend key stored sensitive on Vercel, and the nine templates published in Resend by alias with `web/scripts/publish-resend-templates.mjs`. One templated send to the owner's address went through. | Consumer event emails and the operator card-failure notice leave the platform; the consumer preference toggles govern them. | Complete. Rotate the Resend key from the dashboard if it was ever shared in plain text; the driver only needs a key that can send from `mail.mostfundable.com`. | `web/src/lib/email/resend-driver.ts`; `docs/email-templates/README.md`; Vercel production environment |
| A5 | The credit bureau runs against the bureau test host. Identity checks and report pulls work there today. | Real end-to-end credit flow without production billing from the bureau. | Point `CRS_BASE_URL` at the production host once the bureau grants access. Only the link exposure behaviour changes between hosts. | `web/src/lib/crs/spec-catalog.ts:12-21`; `web/src/lib/crs/sandbox/driver.ts:81-86` |
| A6 | Quick sign-in is off in production: `FEATURE_DEMO_QUICK_SIGN_IN` and `DEMO_QUICK_SIGN_IN_PASSWORD` are not set there (checked 2026-09-05), so the route answers 404. The buttons remain on for local and preview demo walkthroughs. | Demo roles cannot quick-sign-in on the production host. | Remove or re-password the four seeded demo accounts in the hosted database on the day demo access is retired. | `web/src/app/api/auth/quick-sign-in/route.ts:69-79`; Vercel production environment |
| A7 | The funding plan is produced by the fixed rules engine, not the AI: `PLAN_DRIVER` is unset, so it resolves to mock. The narrative written over the plan is live on production since 2026-09-05: `NARRATIVE_DRIVER=openrouter` is set on Vercel (grok-4.3 first attempt, deepseek-v4-flash second, `OPENROUTER_API_KEY` already present) and the site was redeployed. A narrative failure never fails an analysis; the consumer sees template copy instead. | Plan numbers come from rules; the prose over them comes from the model pair and is checked against the plan's own facts before it is stored. | Complete for the narrative. Leave `PLAN_DRIVER` on rules; the AI plan writer still fails `evaluatePlan`. | `web/src/lib/llm/narrative/models.ts:13-18,44-48`; `web/src/lib/analysis/worker.ts:349-380`; `web/src/lib/llm/driver.ts:26-28` |
| A8 | The AI coach answers from six sample articles. | Shows the supervised answer flow safely. | Load real knowledge-base content. The vault-backed source is deliberately blocked until its content has been checked for personal data. | `web/src/lib/kb/fixture-source.ts:5-12`; `web/src/lib/kb/source.ts:44-49,66-68` |

## B. Build gaps found while mapping

| # | Gap | What a user sees today | Fix | Proof |
| --- | --- | --- | --- | --- |
| B1 | The bureau monitoring window is now embedded on the My Credit view. `ConsumerCreditWidget` fetches the preauth token from `/api/monitoring/token` and the frame settings from `/api/monitoring/widget`, hands the token to the frame by `postMessage` to the widget origin only, and renders nothing when either route answers 404. | Still no scores in production: the widget route answers 404 until `CRS_WIDGET_HOST_KEY` is set, so the panel falls back to the durable reading, which returned `available: false` on 2026-09-02. | Set `CRS_WIDGET_HOST_KEY`, have the bureau register the production web address for the widget, then prove one frame loads for an enrolled consumer. | `web/src/components/consumer/credit-widget.tsx:68-114`; `web/src/components/surfaces/consumer.tsx:3037`; `web/src/app/api/monitoring/widget/route.ts:21-24`; `web/src/lib/crs/widget-config.ts:64`; `web/src/components/consumer/credit-widget.test.ts:22-70` |
| B2 | Built. Consumer event email hangs off the in-app delivery row: `runNotificationDispatch` offers every delivered in-app notification to the dispatcher before acknowledging the row, and a failed send leaves the row queued so the job retries both halves. Migration 434 lifted the constraint that pinned `email_enabled` false and seeds credit alerts and team messages on. Migration 440 closed the residual gap: the six event types the feed derives at read time (stage change, plan ready, refresh result, onboarding step, document, team message) now each write a durable `outcome_notifications` row from a trigger on their source table (`stage_history`, `plans`, `enrollment_milestones` and `consents`, `document_uploads`, `support_messages`) in the same transaction as the event, so every one of the eight categories reaches the dispatcher. One unique index per source makes a replay a no-op; the in-app toggle is checked at creation, at opt-out and at dispatch for every category, and an event that arrives while its category is off keeps a tombstone row with no queued work so re-enabling never replays it. The feed itself still derives those six at read time, unchanged. | Toggles save and govern every category. No email leaves until A4 is done, because the dispatcher's gate answers `driver_unset` while the driver is mock. Nothing is backfilled: events recorded before migration 440 were already in the feed and produce no email. `outcome_review_approved` still notifies the operator who owns the client, not the consumer. | A4. | `web/src/lib/notifications/email-dispatch.ts:85-98,142-189`; `web/src/lib/notifications/email-dispatch.server.ts:21-38`; `web/src/lib/ancillary/notifications.ts:42-53`; `supabase/migrations/440_consumer_event_notification_rows.sql`; `supabase/tests/440_consumer_event_notification_rows.test.sql`; `web/src/lib/notifications/email-dispatch.server.test.ts`; `web/src/lib/ancillary/notifications.test.ts:56-90`; `web/src/lib/email/matrix.test.ts:118-135` |
| B3 | Fixed. Nine application templates have a provider contract and one sender each: `operator_card_failure` comes from the billing webhook through the outbox; `consumer_monitoring_alert`, `consumer_stage_change`, `consumer_analysis_complete`, `consumer_refresh_result`, `consumer_enrollment_milestone`, `consumer_document`, `consumer_team_message`, and `consumer_application_update` come from the consumer notification dispatcher, and since migration 440 each of the eight consumer events has a durable delivery row to send from (B2). A CRS alert maps to `consumer_monitoring_alert`; the unused `crs_alert` email template was removed. | No invites or receipts come from the product. Invites come from the sign-in service and receipts come from Stripe. Nothing leaves until A4. | Decide whether invites and receipts stay with the sign-in service and Stripe. | `web/src/lib/email/templates.ts`; `web/src/lib/email/mock-driver.ts`; `web/src/lib/email/matrix.test.ts`; `web/src/lib/email/dispatch.ts`; `web/src/lib/notifications/email-dispatch.ts`; `web/src/lib/notifications/email-dispatch.server.ts` |
| B4 | Fixed. The Plan and billing panel in Settings reads the workspace's own subscription record from the billing route and offers Start subscription or Manage billing through the hosted checkout and portal routes. | Plan, membership, subscription status, billed and included seats, client meter and period end, exactly as the record carries them. Each route refusal has its own plain line, and a mock-driver record says the buttons reach no card. | Complete. Real charges still wait on A1 (Stripe keys and `BILLING_DRIVER=stripe`); until then the buttons open the mock placeholder page. | `web/src/components/operator/plan-billing-panel.tsx`; `web/src/lib/operator/platform-billing.client.ts`; `web/src/components/operator/plan-billing-panel.test.ts` |
| B5 | Removed. The Industry updates tab and its staged-intel queue, which was empty by construction and wrote decisions only to the session, were dropped from the admin surface in commit `beaa1c2`. | No such tab; the admin navigation carries nine views. | Complete. Reintroduce only with a real source behind it. | `web/src/components/surfaces/admin.tsx:183-192`; `git show beaa1c2` |
| B6 | Fixed. The System health panel shows three live checks answered by `/api/admin/health` behind the admin role: the admin database (a head-count read of `orgs`), the background job queue (newest succeeded or skipped tick within 30 minutes, plus failures in the last 24 hours), and driver selection across billing, email, CRS, plan, narrative and assistant, reported degraded while any is on mock. | OK, Degraded or Unknown pill per check with one sentence of detail, never an environment value or key name. | Complete. The checks run at request time only; alerting and recovery ownership stay owner input 5. | `web/src/lib/admin/health.ts:88-175,187-203,295-302`; `web/src/lib/admin/handlers.ts:140-145`; `web/src/components/surfaces/admin.tsx:2437-2500`; `web/src/lib/admin/health.test.ts:62-200`; `web/src/lib/admin/handlers.test.ts:169-190` |
| B7 | Fixed. The operator inbox receives the timeline feature flag. | Upload requests and the conversation timeline appear in the inbox when the flag is on. | Complete. | `web/src/components/surfaces/operator.tsx:9296`; `web/src/components/operator/inbox/inbox-contract.test.ts:104-108` |
| B8 | Fixed. The live Vault schema was read on 2026-09-05: `data_points` requires Vault-only `bank_id`; `bank_datapoints` accepts the mapped `bank_slug`, `dp_type`, optional `date_observed`, and integer `amount`. | Nothing visible until a real delivery is approved. Outbox rows remain recorded when the payload cannot be represented safely. | Vault owner must confirm `dp_type` values, amount units, and provenance/version handling, then approve and prove one delivery. | `web/src/lib/applications/vault-writeback-schema.ts`; `web/src/lib/applications/writeback.ts`; `web/src/lib/applications/writeback.test.ts` |
| B9 | Bureau alert delivery is received, and the receiver is registered on the bureau's development host against the production URL with Basic credentials: a TEST event answered 200 with an acknowledgement and a wrong-credential call answered 401 on 2026-09-05. The bureau's production host has no registration. | Sandbox alerts arrive. Production alerts will not until the production-host registration exists. | Register the receiver on the bureau's production portal when `CRS_BASE_URL` moves to the production host (A5), and prove one alert end to end there. | `web/src/app/api/webhooks/crs/route.ts:16-46`; `web/src/lib/crs/webhook.ts:41-46`; `npm run verify:crs-sandbox` in `web/` |

## C. Code defects to fix before real money or real credit pulls

| # | Defect | Risk | Fix | Proof |
| --- | --- | --- | --- | --- |
| C1 | The sandbox driver now declares report retrieval as per-request billing, so the replay-safety check answers from the driver and a retried analysis with a completed pull operation never buys the report again. | Double bureau billing on a retried analysis. | Complete. Re-declare `cached-read` only when CRS gives this account a written replay guarantee. | `web/src/lib/crs/adapter.ts:44-47`; `web/src/lib/analysis/worker.ts:245`; `supabase/migrations/378_r5c04_crs_pull_operations.sql:6-33` |
| C2 | Fixed. Stripe consumer revenue now comes from retained `invoice.paid` evidence for that subscription and month, rather than the configured price. A Stripe subscription with no paid invoice contributes zero and leaves an explicit incomplete marker without discarding other subscriptions. | Revenue share continues when real payments start, with missing receipts visible. | Complete. | `web/src/lib/billing/paid-invoices.ts`; `supabase/migrations/438_paid_invoice_revenue_evidence.sql`; `web/src/lib/revenue/accruals.ts` |
| C3 | Pull caps are bypassed while the ancillary flag is off, and every bypass now logs `PULL_CAP_BYPASSED_FLAG_OFF` with the client UUID and cause. | A flag flip removes a spend limit without warning. | Complete. Fail closed instead if the flag is ever off in production with real pulls. | `web/src/lib/ancillary/pull-caps.ts:19` |
| C4 | Removed. The unsupported daily analysis schedule job is no longer catalogued or accepted by the background-job queue. | Complete. | No further change. | `web/src/lib/jobs/definitions.ts`; `supabase/migrations/436_remove_analysis_schedule_due_job.sql` |
| C5 | Human-typed support messages are now screened. A client-facing body typed by an operator or admin runs through the same compliance language battery the AI drafts do, inside `sendMessage`, and a hit is refused before the database is touched: the route answers 422 `SUPPORT_MESSAGE_LANGUAGE` with the rule ids that fired, nothing is recorded, and the Inbox tells the operator to remove the flagged phrase. Consumer-authored messages and internal notes (which migration 385 withholds from every consumer) are not screened. | An operator can type prohibited claims to a client. | Complete. Blocked rather than warned, mirroring how a held draft with guardrail flags is never sent. | `web/src/lib/support/service.ts` (`sendMessage`); `web/src/lib/support/errors.ts` (`SupportMessageLanguageError`); `web/src/app/api/support/threads/[id]/messages/route.ts`; `web/src/components/operator/inbox/index.tsx` |
| C6 | Fixed. Manual stage moves are forward-only, one stage at a time: the tracker routine refuses a skip or a backwards move with `stage_transition_not_allowed` (carrying the persisted from and requested to stages), nothing is written, and the API answers 409 with "A client can only move forward one stage at a time." The automatic transitions (enrollment activation, analysis completion, seed) are unchanged, and the application outcome moves to Applying and Funded already step one stage. | An operator can no longer move a client from Onboarding straight to Funded, or back a stage, so stage reports cannot be gamed or mis-clicked. | Complete. A genuine correction backwards now needs the owner, since no surface offers one. | `supabase/migrations/441_manual_stage_transitions.sql`; `supabase/tests/441_manual_stage_transitions.test.sql`; `supabase/tests/050_tracker_stage_engine.sql`; `web/src/lib/tracker/transition.server.ts`; `web/src/app/api/clients/[id]/route.ts` |

## D. Documentation corrections

- The internal engineering context still says the ancillary flag is empty in production. Measured today it is on. All feature flags are on except the email flag, which is moot while no email driver is set.
- Production env pulls through the Vercel CLI return blanks for sensitive values. A blank in a pulled file is not evidence a flag is off. Measure flags by calling a gated route with a signed-in session.
- The Bank Vault holds 47 lenders live. The repository ships 7 fixture lenders; the 47 come from the nightly sync against the real Vault project.

## E. Inputs owed by the project owner

These are unchanged from ASKS-AND-DECISIONS.md, with two additions found in code.

1. Stripe account and approval to charge real cards.
2. Email provider account and an approved sender address.
3. Bureau production approval, plus registration of the production web address for the monitoring widget and the alert receiver.
4. Final public-facing wording.
5. Monitoring and recovery ownership.
6. Walkthrough dates and written acceptance.
7. Real knowledge-base content for the AI coach, added from this review.
8. A decision on launching with the rules-based plan or waiting for the AI plan writer, added from this review.

## Suggested order

Done since the 2026-09-02 measurement: C1, C2, C3, C4, C5, C6, B3, B4, B5, B6, B7 and B8. B2 is live for all eight consumer event types now that A4 is done. The database now runs through migration 441 (438 is C2's paid-invoice evidence; 439 lets the v2 feature extractor persist, which the live-chain check had been failing on; 440 gives the six derived consumer events a durable notification row so their emails can dispatch; 441 is C6's forward-only manual stage rule).

1. Done 2026-09-05: migrations 433 to 441 were pushed to the hosted Supabase project with `supabase db push --db-url` (the hosted database had stopped at 432, so the production deploy had been calling a validator, two billing RPCs and the notification triggers it did not have). Vercel deploys `main` to production on every push and nothing pushes migrations, so every future migration needs the same push; the repository is not `supabase link`ed.
2. Owner inputs 1 to 3, because A1 to A5, A7, B1 and B9 wait on them, and B8 needs the Vault owner to confirm the datapoint vocabulary before one real delivery.
4. B1 before the consumer surface is shown to a paying customer.
5. A6 last, on the day demo access is retired.
